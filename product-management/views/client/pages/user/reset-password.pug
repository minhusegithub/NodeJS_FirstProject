extends ../../layouts/auth.pug
include ../../mixins/alert.pug

block main 
    +alert-error(5000)
    
    .reset-password-page
        .container
            .row.justify-content-center.align-items-center.min-vh-100
                .col-11.col-sm-6.col-md-5.col-lg-4
                    .card.border-0.shadow.rounded-4
                        .card-body.p-4
                            .text-center.mb-4
                                h2.fw-bold.RESET1-css Đặt lại mật khẩu
                                p.text-muted Nhập mật khẩu mới của bạn
                            
                            form(
                                action=`/user/password/reset`
                                method="POST"
                                autocomplete="off"
                            )
                                .form-group.mb-3
                                    input(
                                        type="password"
                                        class="form-control form-control-lg rounded-3 border-0 bg-light"
                                        id="password"
                                        name="password"
                                        placeholder="Mật khẩu mới"
                                        required
                                    )

                                .form-group.mb-3
                                    input(
                                        type="password"
                                        class="form-control form-control-lg rounded-3 border-0 bg-light"
                                        id="confirmPassword"
                                        name="confirmPassword"
                                        placeholder="Nhập lại mật khẩu"
                                        required
                                    )

                                button(
                                    type="submit"
                                    class="btn btn-primary btn-lg w-100 rounded-3"
                                ) Đổi mật khẩu

    style.
        .RESET1-css {
            color: #1E4A7B;
        }

        .reset-password-page {
            background: linear-gradient(135deg, #163a63 0%, #ffffff 100%);
            min-height: 100vh;
        }
        .reset-password-page .card {
            box-shadow: 0 10px 40px rgba(0, 0, 0, 0.2) !important;
        }
        .reset-password-page .form-control {
            padding: 0.8rem 1rem;
            font-size: 15px;
        }
        .reset-password-page .form-control:focus {
            background-color: #fff;
            box-shadow: 0 0 0 3px rgba(30, 74, 123, 0.25);
            border: none;
        }
        .reset-password-page .form-control::placeholder {
            color: #90949c;
        }
        .reset-password-page .btn-primary {
            background: #1E4A7B;
            border: none;
            font-weight: 600;
            padding: 12px;
            transition: all 0.3s ease;
        }
        .reset-password-page .btn-primary:hover {
            transform: translateY(-2px);
            box-shadow: 0 5px 20px rgba(30, 74, 123, 0.4);
            background: #163a63;
        }
        .reset-password-page h2 {
            color: #1E4A7B;
        }
        .reset-password-page .text-muted {
            color: #6c757d !important;
        } 







