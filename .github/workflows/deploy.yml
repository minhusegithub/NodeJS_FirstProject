name: 🚀 Deploy to VPS

on:
  push:
    branches: [main]

jobs:
  deploy:
    name: Deploy to Vultr VPS
    runs-on: ubuntu-latest

    steps:
      - name: 📦 Checkout code
        uses: actions/checkout@v4

      - name: 🔐 Deploy via SSH
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          script: |
            set -e
            echo "📥 Pulling latest code..."
            cd /opt/NodeJS_FirstProject
            sudo git config --global --add safe.directory /opt/NodeJS_FirstProject
            sudo git pull origin main

            echo "🏗️ Building frontend..."
            cd product-management/frontend
            sudo npm install --production=false
            sudo npm run build
            cd ..

            echo "🐳 Rebuilding Docker containers..."
            sudo docker compose up -d --build --remove-orphans

            echo "🔄 Reloading Nginx..."
            sudo cp /opt/NodeJS_FirstProject/product-management/nginx/default.conf /etc/nginx/sites-available/mvnstore
            sudo nginx -t && sudo systemctl reload nginx

            echo "✅ Deployment completed successfully!"
